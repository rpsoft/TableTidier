// import { selectCollectionViewDomain } from '../selectors';

describe('selectCollectionViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

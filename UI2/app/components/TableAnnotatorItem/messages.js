/*
 * TableAnnotator Messages
 *
 * This contains all the text for the TableAnnotator component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableAnnotator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableAnnotator component!',
  },
});

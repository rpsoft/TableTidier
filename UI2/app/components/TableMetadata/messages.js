/*
 * TableMetadata Messages
 *
 * This contains all the text for the TableMetadata component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableMetadata';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableMetadata component!',
  },
});

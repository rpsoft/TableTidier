/*
 * TableNotes Messages
 *
 * This contains all the text for the TableNotes component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableNotes';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableNotes component!',
  },
});

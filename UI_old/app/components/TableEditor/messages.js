/*
 * TableEditor Messages
 *
 * This contains all the text for the TableEditor component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableEditor';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableEditor component!',
  },
});

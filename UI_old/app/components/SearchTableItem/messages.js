/*
 * SearchTableItem Messages
 *
 * This contains all the text for the SearchTableItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearchTableItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchTableItem component!',
  },
});

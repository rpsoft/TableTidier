/*
 * TableResult Messages
 *
 * This contains all the text for the TableResult component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableResult';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableResult component!',
  },
});

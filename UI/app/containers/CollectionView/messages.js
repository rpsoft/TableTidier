/*
 * CollectionView Messages
 *
 * This contains all the text for the CollectionView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CollectionView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CollectionView container!',
  },
});
